# 常见问题及其解决方法

## 1. 编译文件被占用错误

**错误信息示例：**
1. `D:\Project\AI_Education\common\build\intermediates\compile_library_classes_jar\debug\bundleLibCompileToJarDebug\classes.jar: 另一个程序正在使用此文件，进程无法访问。`
2. `D:\Project\AI_Education\app\build\intermediates\compile_and_runtime_not_namespaced_r_class_jar\debug\processDebugResources\R.jar: 另一个程序正在使用此文件，进程无法访问。`

**原因：**
通常由 Gradle 守护进程、Java 进程（如 IDE 索引器）或之前未正常结束的构建任务锁定构建产物导致。Windows 系统对文件锁定机制较为严格，更容易出现此类问题。

**解决方法：**
1.  **停止 Gradle 守护进程**：
    *   命令行运行：`./gradlew --stop`
    *   IDE 操作：在 Gradle 工具窗口点击 "Stop Gradle Daemons" 或重启 IDE。
2.  **强制结束 Java 进程**：
    *   Windows：任务管理器或运行 `taskkill /F /IM java.exe`
    *   Mac/Linux：`pkill -f java` 或 `killall java`
3.  **清理构建缓存**：
    *   命令行运行：`./gradlew clean`
    *   IDE 操作：Build > Clean Project
4.  **手动删除锁定文件**(可选)：
    *   删除报错中提到的文件或整个 build 目录。
