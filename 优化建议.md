# 项目优化建议报告

经过对项目的全面梳理与上一轮的代码重构验证，目前项目在**架构解耦（FeatureApi 路由机制）**、**网络层收敛（统一拦截与结果处理）**、**多模态性能（图片流内存优化与 3D 渲染隔离）**等方面已经具备了很高的合理性和健壮性。

然而，从严谨的 Android 工程规范和极致性能体验的角度来看，当前代码中仍存在一些较深层次的架构与内存管理细节问题。特此总结以下优化建议：

## 1. 状态收集（State Collection）优化
* **问题描述**：在各个模块的 Compose UI 页面（如 `ChatScreen.kt`、`TimelineMapScreen.kt` 等）中，目前广泛使用 `ViewModel.uiState.collectAsState()` 来监听数据流。在 Android 体系下，这会导致即使应用退到后台（不可见状态），UI 仍可能响应数据流的发射而进行不必要的资源消耗。
* **优化建议**：
  * 在 `common` 模块引入 `androidx.lifecycle:lifecycle-runtime-compose` 依赖。
  * 将全局的 `.collectAsState()` 替换为 **`.collectAsStateWithLifecycle()`**，使得 Compose 的状态收集严格遵循 Activity/Fragment 的生命周期（如在 `STOPPED` 时自动暂停收集），节省 CPU 和电池资源。

## 2. 单次事件（Side Effect）处理反模式
* **问题描述**：在类似 `ReviewViewModel`、`TextSummaryViewModel` 中，当前的错误提示或一次性动作是作为 `UiState` 的一部分进行维护的（例如 `val error: String? = null`）。这种做法的隐患在于：当 Compose 页面因其他状态改变发生 Recomposition 时，如果不及时调用 `clearError()`，极易导致 Snackbar 或 Toast 重复弹出。
* **优化建议**：
  * 将**状态（State）**与**事件（Event）**分离。
  * 保持 `UiState` 仅包含页面的持久化状态（如加载中、列表数据）。
  * 引入 `Channel<UiEvent>` 或 `SharedFlow<UiEvent>` 专门用于发送“弹出错误”、“页面跳转”等一次性事件。在 Compose 侧使用 `LaunchedEffect` 配合 `Lifecycle.State.STARTED` 进行一次性消费。

## 3. Bitmap 内存回收（Native Memory Management）
* **问题描述**：虽然之前优化了 `ByteArrayOutputStream` 的内存抖动问题，但在 `SolverViewModel` 的 `encodeImage` 以及 `AiTutorViewModel` 的 `loadScaledBitmap` 方法中，使用了 `Bitmap.createScaledBitmap` 生成压缩图片后，**并未对原始大尺寸的 `Bitmap` 进行 `.recycle()` 回收**。Bitmap 的像素数据存储在 Native 内存中，仅依赖 JVM GC 会导致回收不及时，连续拍照时仍有 OOM 风险。
* **优化建议**：
  * 在生成了 `scaledBitmap` 之后，立即判断 `if (scaledBitmap != originalBitmap) { originalBitmap.recycle() }`，显式释放底层 Native 内存。

## 4. JSON 序列化规范化
* **问题描述**：在 `AiTutorViewModel` 中，为了将多模态对话历史（`ContentItem`）存入数据库，手动使用了 `org.json.JSONObject` 和字符串拼接的方式进行序列化（`serializeContent` / `deserializeContent`）。这不仅代码冗长，且容易在模型字段扩展时引发解析崩溃。
* **优化建议**：
  * 既然项目底层（如 `Retrofit`）已经依赖了 `Gson`（或 `Kotlinx.serialization`），应当统一使用标准的序列化库将 `List<ContentItem>` 转换为 JSON 字符串，提高代码的安全性和可维护性。

## 5. UI 硬编码与国际化（I18n）缺失
* **问题描述**：项目所有的 Compose 页面（超过 20 多个文件，约百余处）中，将中文文本（如 `Text("确认并开始解题")`）直接硬编码在了 UI 层中。
* **优化建议**：
  * 考虑到项目已相对成熟，应当开始将硬编码字符串提取至 `res/values/strings.xml` 中，并在 Compose 中使用 `stringResource(id = R.string.xxx)`。这不仅是 Android 开发的基本规范，也能为未来扩展多语言（如英文版）奠定基础。
