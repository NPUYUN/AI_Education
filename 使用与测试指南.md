# 东大智搭 (Dongda Zhida) - 使用与测试指南

本文档旨在帮助开发者、测试人员和最终用户从零开始搭建运行环境，系统地测试应用内的各项核心功能，并提供安全、可控的大语言模型 (LLM) 提示词 (Prompt) 优化方案，确保未来的所有修改和二次开发都属于“小型迭代”，而不会破坏核心架构的稳定性。

---

## 🛠️ 第一部分：开发环境搭建

### 1. 下载并安装 Android Studio
1. 访问 [Android Studio 官方下载页面](https://developer.android.com/studio)。
2. 下载最新版本（推荐使用 **Ladybug** 或更高版本）。
3. 安装过程中，请勾选 **Android SDK**、**Android SDK Platform-Tools** 和 **Android Virtual Device (AVD)**。
4. 建议将 Android SDK 安装在非系统盘（如 `D:\Android\Sdk`），路径中**不要包含中文或空格**。

### 2. 配置 JDK 与 SDK 环境
1. 打开 Android Studio，进入 `Settings` (Windows) 或 `Preferences` (Mac/Linux)。
2. 搜索 `SDK Manager`，确保已下载 **Android API 34**（或更高）的 SDK Platform。
3. 进入 `Build, Execution, Deployment` -> `Build Tools` -> `Gradle`，确保 **Gradle JDK** 设置为 **JDK 17**（应用要求最低 Java 17 编译环境）。

### 3. 克隆或导入项目
1. 在 Android Studio 的欢迎界面，点击 **Open**，选择本项目所在的根目录 `AI_Education`。
2. 等待 Gradle 自动下载依赖并完成初次同步 (Sync)。如果同步失败，请检查网络设置（如需代理，请在 `gradle.properties` 中配置）。

### 4. 配置 API Key（核心步骤）
本项目依赖外部大模型（如通义千问 Qwen）。为了安全，代码中不直接硬编码 Key。
请在项目根目录下找到或新建一个名为 `local.properties` 的文件，填入以下内容：

```properties
# 您的实际 API Key
API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxx
# 大模型 API 的基础 URL（请确保以 / 结尾）
BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1/
# 默认使用的模型名称
MODEL_NAME=qwen-vl-plus
```

### 5. 编译与运行
1. 点击顶部工具栏的绿色三角形 **Run 'app'** 按钮。
2. 选择一个已连接的 Android 物理设备或运行中的模拟器（建议 Android 8.0/API 26 以上）。
3. 如果遇到端口被占用或缓存问题，可在根目录运行 `clean_project.bat`（Windows）进行深度清理后重试。

---

## 🧪 第二部分：核心功能测试清单

为确保项目各个模块运转正常，请按照以下步骤进行系统测试。

### 1. AI 辅导 (AI Tutor)
* **对话测试**：进入“AI辅导”主页，尝试发送文字消息，检查 LLM 是否能够流式回复。
* **侧边栏测试**：点击左上角的“汉堡菜单”图标，检查个人中心侧边栏是否正常滑出，各项配置项是否可点击。
* **历史时间轴地图**：通过指令触发时间轴地图（如果实现了特定入口），检查地图加载是否正常，双指缩放是否流畅，标记点点击是否能弹出详情。

### 2. 智能解题 (Solver)
* **界面UI校验**：点击底部导航栏“解题”，检查顶部导航栏标题是否居中显示为“解题”，且**没有**左上角的多余菜单图标。
* **多模态输入**：
  - 点击“拍照解题”，测试相机唤起、拍照、裁剪及预览功能。
  - 点击“上传题目”，测试从相册选择图片。
* **双视界解析校验**：输入测试题目（如一段物理描述），点击“开始解题”。
  - 检查结果页是否正常分离显示“题目内容”、“最终答案”。
  - **关键校验**：检查最终答案中的文字字体大小是否正常（不能像被缩小成蚂蚁字）。
  - 点击“查看解题详情”，检查弹窗内是否展示了完整的推理过程，并且 Markdown（含数学公式）渲染是否正常。
* **历史记录与错题本联动**：
  - 解题完成后，点击“收录至错题本”。
  - 返回解题主页，查看历史解题记录，检查题目描述是否已精简为 20 字以内的【核心考点】（如“【核心考点】牛顿第二定律的应用...”）。
  - 点击历史记录条目，检查详情弹窗是否正常渲染，底部是否有“收录至错题本”按钮。

### 3. 智能总结 (Summarizer)
* **界面UI校验**：点击底部“总结”，检查顶部标题是否居中为“总结”，无多余左侧菜单图标。
* **功能分支测试**：
  - **文本总结**：粘贴长文本或上传 PDF/Word 文档，测试能否生成结构化的金字塔总结。
  - **音视频总结**：选择一段音频或视频，测试离线语音识别 (Sherpa ONNX) 是否正常转写（无崩溃闪退），并生成最终大纲。
  - **对话总结**：选择一段 AI 辅导的历史对话进行总结复盘。

### 4. 智能复习 (Review)
* **错题本折叠与展开**：进入“错题本”，检查错题卡片是否默认折叠（仅显示前两行文本）。点击“点击查看详情”后，检查卡片是否平滑展开并完整渲染 Markdown 与 Latex 公式。
* **一键变式测试**：
  - 选中 1-2 道错题，点击底部的“一键测试”。
  - 在弹窗中选择题目数量，确认生成。
  - 检查生成的 JSON 变式题是否成功解析，进入沉浸式单题作答界面。
  - 测试“上一题”、“下一题”、悬浮“答题卡”及最终的“交卷”流程。
  - 交卷后检查 AI 批改得分与解析，测试能否将生成的错题再次“加入错题本”。
* **历史复习追溯**：在原错题卡片下方点击“查看历史复习”，检查列表是否展示该题的所有历史测试得分与正确率，测试侧滑删除功能。

---

## 🧠 第三部分：AI 提示词 (Prompt) 优化与修改指南

为了使得非研发人员或 Prompt 工程师能够快速调整 AI 的回复风格、格式和行为，本项目将所有的核心 Prompt 统一集中管理。
**注意：在此处修改 Prompt 是安全的，不会破坏底层核心业务逻辑（只要不违反结构性约束）。**

### 1. 提示词存放位置
所有的系统级提示词均定义在：
👉 `common/src/main/java/com/example/common/config/AppConstants.kt`

在该文件中，你可以找到诸如 `SOLVER_GEOMETRY_SYSTEM_PROMPT`（几何解题）、`SOLVER_PHYSICS_SYSTEM_PROMPT`（物理解题）、`TEXT_SUMMARY_SYSTEM_PROMPT`（文本总结）等常量。

### 2. 安全修改指南（Do's & Don'ts）
为了防止修改 Prompt 导致客户端崩溃或渲染异常，请严格遵守以下**铁律**：

✅ **允许的修改（Do's）**
* **修改角色设定**：例如你可以把 AI 设定为“严厉的老师”或“幽默的学长”，改变它的语气和用词风格。
* **调整输出内容的详略**：你可以让它在【严密推导】环节中增加更多的推导细节，或者要求它举出生活中的例子。
* **增加新的提取字段（如果客户端不强制要求）**：比如要求它多输出一段【学习建议】。

❌ **绝对禁止的修改（Don'ts）—— 否则会破坏核心系统**
1. **禁止破坏方括号标题结构**：
   客户端依赖正则匹配来拆分答案。如解题模块依赖 `【题目内容】`、`【最终答案】`、`【考点提炼】` 等固定标题进行截取。**绝对不能修改或删除这些带有【】的标题文字**。
2. **禁止使用块级公式包裹普通中文字符**：
   **核心警告**：在要求 AI 输出最终答案或任何包含中文的段落时，**绝对不能使用 `$$...$$` 或 `\[...\]`** 将整个段落包裹起来！否则客户端的 Markdown 渲染器会将其当做一张巨大的单行图片，导致整体被强行缩放，字体变得像蚂蚁一样小。
   *正确示范*：请在 Prompt 中强调“使用行内公式 `$ ... $` 与普通文本混合排版”。
3. **禁止破坏动态绘图的 JSON 格式**：
   如果 Prompt 中包含绘图指令（如几何、物理绘图），**绝对不能删除** `BEGIN_DRAWING_JSON` 与 `END_DRAWING_JSON` 标签。输出的内容必须是一个纯正的 JSON Array，不能包含 Markdown 的 ````json` 代码块包裹，也不能在标签外说废话。

---

## 🛡️ 第四部分：后续维护与“防劣化”原则

本项目基于 Clean Architecture (整洁架构) 和 MVI (单向数据流) 打造。为了确保后续接手人员进行二次开发时不会将项目改“乱”，请遵守以下维护原则：

### 1. UI 层的修改必须隔离
所有的界面布局调整（如颜色、边距、字体、图标替换）只能在 `presentation/screens` 或 `presentation/components` 包下的 `.kt` 文件中进行。
**严禁**在 UI 的 Composable 函数中直接编写业务逻辑（如直接请求网络、直接操作数据库）。所有行为必须通过调用 `viewModel.doSomething()` 发送意图。

### 2. 数据状态的单向流动
如果需要在界面上新增一个状态（如新增一个 Loading 圈，或者新增一个输入框），流程如下：
1. 打开对应的 ViewModel（如 `SolverViewModel`）。
2. 在它的 `UiState` 数据类中新增一个字段（如 `val isExtraLoading: Boolean = false`）。
3. 在 ViewModel 中通过 `_uiState.update { it.copy(isExtraLoading = true) }` 更新状态。
4. 在 UI 层通过 `uiState.isExtraLoading` 读取并渲染。
**严禁**绕过 ViewModel 在 UI 层自建 MutableState 来维护核心业务数据，这会导致横竖屏切换或导航返回时数据丢失。

### 3. 顶栏 (TopBar) 的统一规范
为了解决不同页面之间导航栏样式不统一、甚至出现“双重标题”的问题，项目已在最外层的宿主 `MainScreen.kt` 中统一接管了底部主导航栏对应页面（AI辅导、解题、总结、复习）的 `CenterAlignedTopAppBar`。
* **新增主页面时**：不需要在子页面的 Scaffold 中重复写 `topBar = {...}`，否则会出现两个标题重叠。
* **新增全屏子页面时**（如解题详情弹窗、作答界面等非底部导航页面）：可以且应该在该子页面的 Scaffold 中自行定义返回用的 `TopAppBar`。

---
严格遵循上述测试与开发指南，可以确保东大智搭 (Dongda Zhida) 持续保持高质量、高稳定性与优异的用户体验。祝开发顺利！🚀